if insysmsg "Now tracking:"
    overhead "::: RED FOUND :::"
    overhead "::: RECALLING NOW :::"
    cast "recall"
    waitfortarget 3000
    if findtype 'recall rune'
        targettype 'recall rune' backpack
    endif
    if findtype 'Runebook'
        targettype 'Runebook' backpack
    endif
    overhead "::: Recalled Out :::"
endif

//REEQUIP SO THEIVES CANT STEAL YOUR TOOLS
//SET TO YOUR TOOL
//OR COMMENT SECTION OUT
if rhandempty
    if findtype 'pickaxe' backpack as pickaxe
        dclick 'pickaxe'               
    endif
endif
clearsysmsg 
replay