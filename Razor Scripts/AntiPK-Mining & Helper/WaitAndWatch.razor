if insysmsg "Now tracking:"
    overhead "::: RED FOUND :::"
    overhead "::: RECALLING NOW :::"
    cast "recall"
    waitfortarget 3000
    if findtype 'recall rune' in backpack
        targettype 'recall rune' in backpack
    endif
    if findtype 'Runebook' in backpack
        targettype 'Runebook' in backpack
    endif
    overhead "::: Recalled Out :::"
endif
replay