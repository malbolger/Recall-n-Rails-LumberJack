if skill 'Peacemaking' < 100
    useskill 'Peacemaking'
    if insysmsg 'you must wait a few moments'
        pause 1000
        replay
    endif
    waitfortarget 1000
    if insysmsg 'what instrument'
        if findtype 'drum'
            dclicktype 'drum'
            replay
        endif
        if findtype 'lap harp'
            dclicktype 'lap harp'
            replay
        endif
        if findtype 'tamborine'
            dclicktype 'tamborine'
            replay
        endif
        if findtype 'lute'
            dclicktype 'lute'
            replay
        endif
        replay
    endif
    targettype 'a sand crawler'
    pause 10000
    replay
endif
if skill 'Discordance' < 100
    useskill 'Discordance'
    waitfortarget 1000
    if insysmsg 'what instrument'
        if findtype 'drum'
            dclicktype 'drum'
            replay
        endif
        if findtype 'lap harp'
            dclicktype 'lap harp'
            replay
        endif
        if findtype 'tamborine'
            dclicktype 'tamborine'
            replay
        endif
        if findtype 'lute'
            dclicktype 'lute'
            replay
        endif
        replay
    endif
    targettype 'a sand crawler'
    pause 10000
    replay
endif
